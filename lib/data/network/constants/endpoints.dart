class Endpoints {
  Endpoints._();

  // base url
  static const String baseUrlApiUrl = "https://free.currconv.com";

  // receiveTimeout
  static const int receiveTimeout = 20000;

  // connectTimeout
  static const int connectionTimeout = 30000;
}
